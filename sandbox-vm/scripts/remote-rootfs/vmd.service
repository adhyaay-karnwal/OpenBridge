[Unit]
Description=vmd sandbox daemon (remote mode)
After=network.target

[Service]
Type=simple
Environment=VMD_REMOTE=1
Environment=VMD_LISTEN_ADDR=:50051
ExecStart=/usr/local/bin/vmd
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
